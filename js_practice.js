function golf(par, strokes) {
	if (strokes == 1) {
		return name;
	}
}

function caseInSwitch(val) {
	var answer = ""
	switch(val) {
		case 1:
			answer = "alpha";
			break;
		case 2:
			answer = "beta";
			break;
	}
	return answer;
}

function caseInSwitch2(val) {
	var answer = ""
	switch(val) {
		case 1:
		case 2:
		case 3:
			answer = "Low";
			break;
		case 4:
		case 5:
		case 6:
			answer = "High";
			break;
	}
	return answer;
}

function abTest(a,b) {
	if (a < 0 || b < 0 ) {
		return undefined;
	}
	return Math.round(Math)
}


var OurDog = {
	"name" : "Potato"
	"friends": ["everything!"]
};

var myDog = {
	"name": "Turnip"
	"friends": []
};

myDog.bark = "woof";
myDog['bark'] = "arf";

delete myDog.bark;


var lookup = {
	"alpha" : "Adams",
	"bravo" : "Boston",
};

result = lookup[val]

var myObj = {
	gift: "pony"
}

function checkObj(checkProp) {
	if (myObj.hasPropery(checkProp)) {
		return myObj[checkProp]
	}
}